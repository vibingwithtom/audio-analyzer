{
  "name": "audio-analyzer",
  "version": "1.0.0",
  "type": "module",
  "description": "Audio file analyzer with Chrome extension and desktop application",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "dev": "npm run dev --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "keywords": [
    "audio",
    "analyzer",
    "chrome-extension",
    "electron",
    "desktop",
    "web-app"
  ],
  "author": "vibingwithtom",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/vibingwithtom/audio-analyzer.git"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "gh-pages": "^6.3.0"
  },
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "fluent-ffmpeg": "^2.1.3"
  }
}
